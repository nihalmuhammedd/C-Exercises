#include <unistd.h>

void ft_sort(char **arg)
{
}
int main(int arg, char **argv)
{
    int len;
    int j;

    len = 0;
    while (argv[len] != '\0')
        len++;
    ft_sort(argv); 
    j = 0;
    if (arg == 2)
    {
        while (argv[1][j] != '\0')
        {
                
        }
    }
}